# Mostly generated with Claude Sonnet 4

name: Build and Deploy

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - uses: actions/configure-pages@vN
      id: config-pages # Gets us the base url in advance
    - name: Generate site
      env:
        BASE_URL: ${{steps.config-pages.outputs.page_url}}
      run: python 5_create_static.py data/ web/ dist/ ${BASE_URL}

    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: dist/

    - name: Deploy to Pages
      uses: actions/deploy-pages@v4
